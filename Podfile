# Uncomment the next line to define a global platform for your project
  platform :ios, '9.0'

target 'CRUD app' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CRUD app

  target 'CRUD appTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CRUD appUITests' do
    inherit! :search_paths
    # Pods for testing
  end
	pod 'SwiftMessages'

end
